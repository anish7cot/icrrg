**Executive Summary**The security scan of the **acme-webapp** repository (80 commits, 2026‑03‑17 → 2026‑03‑31) uncovered **285 findings** with an average risk score of **9.14/10**. Critical and high‑severity issues dominate the results, indicating that immediate remediation is required to prevent credential exposure, injection attacks, and unauthorized access.

---

### Critical & High Findings  

| Finding type | Approx. count | Risk description | Typical affected files (top‑5) | Remediation |
|--------------|---------------|------------------|--------------------------------|-------------|
| **review:sql_injection** | 18 | Unsanitized user input concatenated into SQL queries enables attackers to read, modify, or delete data. | `src/api/payments.py`, `src/api/users.py`, `src/db/migrations/002_add_users.sql`, `src/services/notification.py`, `src/auth/login.py` | Use parameterised queries or an ORM; replace string‑building with placeholders (e.g., `cursor.execute("SELECT … WHERE id = %s", (user_id,))`). Add automated tests that attempt injection payloads. |
| **secret:github_token** | 17 | Exposed personal‑access tokens grant attackers read/write access to repositories and can be used to pivot to internal systems. | `.env.example`, `src/config/settings.py`, `src/utils/crypto.py`, `src/services/email_service.py`, `src/auth/jwt_utils.py` | Rotate the token immediately, remove it from the repo, and add `.env*` to `.gitignore`. Store tokens in a secret manager (e.g., AWS Secrets Manager, HashiCorp Vault) and inject them at runtime. |
| **secret:generic_api_key** | 16 | Hard‑coded API keys allow unauthorized use of third‑party services, leading to abuse, cost overruns, or data leakage. | `.env.example`, `src/config/settings.py`, `src/services/notification.py`, `src/api/health.py`, `src/utils/crypto.py` | Rotate each key, delete the plaintext values from history (`git filter‑repo` or `bfg`), and reference them via environment variables or a secret‑management service. |
| **review:hardcoded_secret** | 19 | Plaintext secrets (passwords, salts, JWT signing keys) in source code make them trivial to extract. | `src/auth/jwt_utils.py`, `src/utils/crypto.py`, `src/config/settings.py`, `src/services/email_service.py`, `src/models/user.py` | Move secrets out of code; load them from environment variables or a vault. For JWT, generate a strong random signing key at deployment and store it securely. |
| **review:open_redirect** | 21 | Unvalidated redirect URLs let attackers craft phishing links that appear to originate from the app. | `src/api/users.py`, `src/auth/login.py`, `src/middleware/cors.py`, `src/services/notification.py`, `src/api/health.py` | Validate redirect targets against an allow‑list of internal routes; use `urlparse` to ensure the netloc matches the app’s domain. |
| **review:xss** | 16 | Unescaped user‑controlled data rendered in HTML enables script execution in victims’ browsers. | `src/api/payments.py`, `src/api/users.py`, `src/services/notification.py`, `src/auth/login.py`, `src/services/email_service.py` | Apply context‑aware escaping (e.g., Jinja2 autoescape, React’s JSX, or `html.escape`). Implement a CSP header that disallows inline scripts. |
| **review:missing_auth** | 15 | Endpoints lacking proper authentication checks expose sensitive data or functionality to unauthenticated users. | `src/api/users.py`, `src/api/payments.py`, `src/auth/login.py`, `src/services/notification.py`, `src/api/health.py` | Enforce authentication middleware on all routes that touch user data; add unit tests that assert 401/403 for anonymous requests. |
| **review:logging_sensitive** | 18 | Logs containing passwords, tokens, or PII increase the impact of log‑leakage incidents. | `src/utils/crypto.py`, `src/services/email_service.py`, `src/auth/jwt_utils.py`, `src/config/settings.py`, `src/models/user.py` | Sanitize log output: replace secrets with `[REDACTED]` before logging. Use a logging filter or structlog processor that drops fields matching secret patterns. |
| **review:insecure_hash** | 15 | Use of weak hash functions (MD5, SHA‑1) for password storage or integrity checks facilitates collision attacks. | `src/utils/crypto.py`, `src/auth/jwt_utils.py`, `src/services/notification.py`, `src/config/settings.py`, `src/models/user.py` | Replace with bcrypt, Argon2id, or PBKDF2 for passwords; for integrity, use HMAC‑SHA256. |
| **review:weak_crypto** | 13 | Use of deprecated algorithms (e.g., DES, RC4) or small key sizes weakens encryption. | `src/utils/crypto.py`, `src/config/settings.py`, `src/services/email_service.py`, `src/auth/jwt_utils.py`, `src/models/user.py` | Migrate to AES‑256‑GCM for symmetric encryption and RSA‑2048+ or ECDSA‑P‑256 for asymmetric keys. Disable TLS 1.0/1.1 in server configs. |
| **secret:private_key** | 12 | Exposure of private SSH/TLS keys permits attackers to impersonate services or decrypt traffic. | `.env.example`, `src/utils/crypto.py`, `src/config/settings.py`, `src/services/email_service.py`, `src/auth/jwt_utils.py` | Rotate the key pair, remove the private key from the repo, and store it in a hardware security manager or secret vault. Ensure the corresponding public key is redistributed to trusted parties. |
| **review:path_traversal** | 12 | Insufficient validation of file‑path inputs lets attackers read/write arbitrary files on the host. | `src/api/users.py`, `src/api/payments.py`, `src/services/notification.py`, `src/auth/login.py`, `src/services/email_service.py` | Normalise paths with `os.path.normpath` and verify they reside within an allowed base directory (`os.path.commonpath`). Reject paths